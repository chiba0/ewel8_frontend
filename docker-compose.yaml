version: '3.9'
services:
  vue-app:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    environment:
      - NODE_ENV=development
    volumes:
    - ./vueApp:/usr/src/app
    command: sh -c "cd vue-sample && yarn run serve"
    ports:
    - "18080:8080"